# Происхождение исходных данных

`triangle_lab1.py` — неизменённая копия `triangle.py` из коммита
`1d54bae` ветки `Lab_1` репозитория `leuff92/PTPM-VKI`.
Она нужна только для воспроизведения шести падений до исправления своего кода.

`../Delivery.py` и `../README.md` сохранены из коммита
`133f24ade02c2df6ed2b040b0b27651c48da2673` ветки `Lab_2`.
Номера строк в отчёте относятся именно к этой версии `Delivery.py`.

Протоколы получены командой `python scripts/run_checks.py`.
Контроль исходного решения ЛР1 — `python scripts/run_checks.py --baseline`.
